// import { isValidPassword } from '../../shared/utils/isPass.util';
//
// export const RegisterValidate = (fullName: string, email: string, password: string, confirmPassword: string): string => {
//     if (!fullName) return 'Họ và tên rỗng!';
//     if (!email) return 'email rỗng!'
//     if (!password) return 'Mật khẩu rỗng!';
//     if (!confirmPassword) return 'Mật khẩu xác thực rỗng!';
//     if (!isValidPassword(password)) return 'Mật khẩu không đúng định dạng yêu cầu!'
//     if (password !== confirmPassword) return 'Không khớp mật khẩu xác thực!'
//     return '';
// }